import { Grid } from "@mui/material";
import React from "react";

export const SearchBox = () => {
  return (
    <Grid container spacing={2}>
      <Grid>돋보기 아이콘</Grid>
      <Grid>검색창 인풋</Grid>
    </Grid>
  );
};
